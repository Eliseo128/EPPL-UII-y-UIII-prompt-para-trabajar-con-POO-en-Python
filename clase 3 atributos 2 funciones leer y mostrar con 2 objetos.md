Aquí tienes 25 ejercicios diseñados con enfoque didáctico, siguiendo tu estructura (clase, atributos, funciones y objetos), listos para trabajar de forma interactiva en Python:

---

1. Crear la clase **Alumno**, con 3 atributos (nombre, matrícula, promedio), 2 funciones leerdatos(), mostrardatos() y 2 objetos (alumno1, alumno2).

2. Crear la clase **Empleado**, con 3 atributos (nombre, puesto, salario), 2 funciones leerdatos(), mostrardatos() y 2 objetos (empleado1, empleado2).

3. Crear la clase **Doctor**, con 3 atributos (nombre, especialidad, cédula), 2 funciones leerdatos(), mostrardatos() y 2 objetos (doctor1, doctor2).

4. Crear la clase **Paciente**, con 3 atributos (nombre, edad, diagnóstico), 2 funciones leerdatos(), mostrardatos() y 2 objetos (paciente1, paciente2).

5. Crear la clase **Producto**, con 3 atributos (nombre, precio, stock), 2 funciones leerdatos(), mostrardatos() y 2 objetos (producto1, producto2).

6. Crear la clase **Vehiculo**, con 3 atributos (marca, modelo, año), 2 funciones leerdatos(), mostrardatos() y 2 objetos (vehiculo1, vehiculo2).

7. Crear la clase **Libro**, con 3 atributos (título, autor, editorial), 2 funciones leerdatos(), mostrardatos() y 2 objetos (libro1, libro2).

8. Crear la clase **CuentaBancaria**, con 3 atributos (titular, número_cuenta, saldo), 2 funciones leerdatos(), mostrardatos() y 2 objetos (cuenta1, cuenta2).

9. Crear la clase **Profesor**, con 3 atributos (nombre, materia, antigüedad), 2 funciones leerdatos(), mostrardatos() y 2 objetos (profesor1, profesor2).

10. Crear la clase **Mascota**, con 3 atributos (nombre, especie, edad), 2 funciones leerdatos(), mostrardatos() y 2 objetos (mascota1, mascota2).

11. Crear la clase **Computadora**, con 3 atributos (marca, procesador, RAM), 2 funciones leerdatos(), mostrardatos() y 2 objetos (pc1, pc2).

12. Crear la clase **Celular**, con 3 atributos (marca, modelo, almacenamiento), 2 funciones leerdatos(), mostrardatos() y 2 objetos (celular1, celular2).

13. Crear la clase **Curso**, con 3 atributos (nombre, duración, costo), 2 funciones leerdatos(), mostrardatos() y 2 objetos (curso1, curso2).

14. Crear la clase **Cliente**, con 3 atributos (nombre, correo, teléfono), 2 funciones leerdatos(), mostrardatos() y 2 objetos (cliente1, cliente2).

15. Crear la clase **Pedido**, con 3 atributos (folio, producto, cantidad), 2 funciones leerdatos(), mostrardatos() y 2 objetos (pedido1, pedido2).

16. Crear la clase **Hotel**, con 3 atributos (nombre, ubicación, estrellas), 2 funciones leerdatos(), mostrardatos() y 2 objetos (hotel1, hotel2).

17. Crear la clase **Habitacion**, con 3 atributos (número, tipo, precio), 2 funciones leerdatos(), mostrardatos() y 2 objetos (habitacion1, habitacion2).

18. Crear la clase **Factura**, con 3 atributos (folio, cliente, total), 2 funciones leerdatos(), mostrardatos() y 2 objetos (factura1, factura2).

19. Crear la clase **Película**, con 3 atributos (título, género, duración), 2 funciones leerdatos(), mostrardatos() y 2 objetos (pelicula1, pelicula2).

20. Crear la clase **Cancion**, con 3 atributos (título, artista, duración), 2 funciones leerdatos(), mostrardatos() y 2 objetos (cancion1, cancion2).

21. Crear la clase **Deporte**, con 3 atributos (nombre, tipo, jugadores), 2 funciones leerdatos(), mostrardatos() y 2 objetos (deporte1, deporte2).

22. Crear la clase **Ciudad**, con 3 atributos (nombre, población, país), 2 funciones leerdatos(), mostrardatos() y 2 objetos (ciudad1, ciudad2).

23. Crear la clase **Restaurante**, con 3 atributos (nombre, tipo_comida, ubicación), 2 funciones leerdatos(), mostrardatos() y 2 objetos (rest1, rest2).

24. Crear la clase **Evento**, con 3 atributos (nombre, fecha, lugar), 2 funciones leerdatos(), mostrardatos() y 2 objetos (evento1, evento2).

25. Crear la clase **Juego**, con 3 atributos (nombre, plataforma, género), 2 funciones leerdatos(), mostrardatos() y 2 objetos (juego1, juego2).

---

Aquí tienes 35 ejercicios adicionales, continuando la numeración del 26 al 60 y manteniendo la misma estructura didáctica:

---

26. Crear la clase **Proveedor**, con 3 atributos (nombre, empresa, teléfono), 2 funciones leerdatos(), mostrardatos() y 2 objetos (proveedor1, proveedor2).

27. Crear la clase **Inventario**, con 3 atributos (producto, cantidad, ubicación), 2 funciones leerdatos(), mostrardatos() y 2 objetos (inventario1, inventario2).

28. Crear la clase **CitaMedica**, con 3 atributos (paciente, fecha, hora), 2 funciones leerdatos(), mostrardatos() y 2 objetos (cita1, cita2).

29. Crear la clase **Seguro**, con 3 atributos (tipo, cobertura, costo), 2 funciones leerdatos(), mostrardatos() y 2 objetos (seguro1, seguro2).

30. Crear la clase **TarjetaCredito**, con 3 atributos (titular, número, límite), 2 funciones leerdatos(), mostrardatos() y 2 objetos (tarjeta1, tarjeta2).

31. Crear la clase **OrdenCompra**, con 3 atributos (folio, proveedor, total), 2 funciones leerdatos(), mostrardatos() y 2 objetos (orden1, orden2).

32. Crear la clase **Envio**, con 3 atributos (destino, peso, costo), 2 funciones leerdatos(), mostrardatos() y 2 objetos (envio1, envio2).

33. Crear la clase **Biblioteca**, con 3 atributos (nombre, dirección, teléfono), 2 funciones leerdatos(), mostrardatos() y 2 objetos (biblio1, biblio2).

34. Crear la clase **PrestamoLibro**, con 3 atributos (usuario, libro, fecha), 2 funciones leerdatos(), mostrardatos() y 2 objetos (prestamo1, prestamo2).

35. Crear la clase **Usuario**, con 3 atributos (nombre, contraseña, rol), 2 funciones leerdatos(), mostrardatos() y 2 objetos (usuario1, usuario2).

36. Crear la clase **Rol**, con 3 atributos (nombre, permisos, descripción), 2 funciones leerdatos(), mostrardatos() y 2 objetos (rol1, rol2).

37. Crear la clase **Permiso**, con 3 atributos (nombre, nivel, descripción), 2 funciones leerdatos(), mostrardatos() y 2 objetos (permiso1, permiso2).

38. Crear la clase **Asistencia**, con 3 atributos (nombre, fecha, estado), 2 funciones leerdatos(), mostrardatos() y 2 objetos (asistencia1, asistencia2).

39. Crear la clase **Calificacion**, con 3 atributos (alumno, materia, nota), 2 funciones leerdatos(), mostrardatos() y 2 objetos (calif1, calif2).

40. Crear la clase **Materia**, con 3 atributos (nombre, clave, créditos), 2 funciones leerdatos(), mostrardatos() y 2 objetos (materia1, materia2).

41. Crear la clase **Horario**, con 3 atributos (día, hora_inicio, hora_fin), 2 funciones leerdatos(), mostrardatos() y 2 objetos (horario1, horario2).

42. Crear la clase **Transporte**, con 3 atributos (tipo, capacidad, costo), 2 funciones leerdatos(), mostrardatos() y 2 objetos (transporte1, transporte2).

43. Crear la clase **Ruta**, con 3 atributos (origen, destino, distancia), 2 funciones leerdatos(), mostrardatos() y 2 objetos (ruta1, ruta2).

44. Crear la clase **TicketSoporte**, con 3 atributos (folio, problema, estado), 2 funciones leerdatos(), mostrardatos() y 2 objetos (ticket1, ticket2).

45. Crear la clase **ComputadoraPortatil**, con 3 atributos (marca, batería, peso), 2 funciones leerdatos(), mostrardatos() y 2 objetos (laptop1, laptop2).

46. Crear la clase **Impresora**, con 3 atributos (marca, tipo, velocidad), 2 funciones leerdatos(), mostrardatos() y 2 objetos (impresora1, impresora2).

47. Crear la clase **Red**, con 3 atributos (nombre, tipo, velocidad), 2 funciones leerdatos(), mostrardatos() y 2 objetos (red1, red2).

48. Crear la clase **Sensor**, con 3 atributos (tipo, valor, unidad), 2 funciones leerdatos(), mostrardatos() y 2 objetos (sensor1, sensor2).

49. Crear la clase **Alarma**, con 3 atributos (tipo, estado, ubicación), 2 funciones leerdatos(), mostrardatos() y 2 objetos (alarma1, alarma2).

50. Crear la clase **UsuarioWeb**, con 3 atributos (usuario, correo, contraseña), 2 funciones leerdatos(), mostrardatos() y 2 objetos (web1, web2).

51. Crear la clase **Publicacion**, con 3 atributos (título, contenido, autor), 2 funciones leerdatos(), mostrardatos() y 2 objetos (post1, post2).

52. Crear la clase **Comentario**, con 3 atributos (usuario, texto, fecha), 2 funciones leerdatos(), mostrardatos() y 2 objetos (coment1, coment2).

53. Crear la clase **Mensaje**, con 3 atributos (emisor, receptor, contenido), 2 funciones leerdatos(), mostrardatos() y 2 objetos (mensaje1, mensaje2).

54. Crear la clase **Chat**, con 3 atributos (usuario1, usuario2, mensajes), 2 funciones leerdatos(), mostrardatos() y 2 objetos (chat1, chat2).

55. Crear la clase **Video**, con 3 atributos (título, duración, resolución), 2 funciones leerdatos(), mostrardatos() y 2 objetos (video1, video2).

56. Crear la clase **Suscripcion**, con 3 atributos (usuario, plan, precio), 2 funciones leerdatos(), mostrardatos() y 2 objetos (sub1, sub2).

57. Crear la clase **Pago**, con 3 atributos (monto, fecha, método), 2 funciones leerdatos(), mostrardatos() y 2 objetos (pago1, pago2).

58. Crear la clase **Reserva**, con 3 atributos (cliente, fecha, número_personas), 2 funciones leerdatos(), mostrardatos() y 2 objetos (reserva1, reserva2).

59. Crear la clase **Vuelo**, con 3 atributos (origen, destino, hora), 2 funciones leerdatos(), mostrardatos() y 2 objetos (vuelo1, vuelo2).

60. Crear la clase **Aerolinea**, con 3 atributos (nombre, país, flota), 2 funciones leerdatos(), mostrardatos() y 2 objetos (aero1, aero2).

---


