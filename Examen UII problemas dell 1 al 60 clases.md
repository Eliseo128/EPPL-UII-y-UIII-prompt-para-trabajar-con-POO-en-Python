¡Claro que sí! Como tu profesor, he preparado una lista de 30 ejercicios prácticos para que refuerces la estructura de clases, atributos, métodos y creación de objetos en Programación Orientada a Objetos.

Aquí tienes los enunciados:

1. Crear la clase "Libro", con 3 atributos (título, autor, numPaginas), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (libro1, libro2).
2. Crear la clase "Persona", con 3 atributos (nombre, edad, dni), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (persona1, persona2).
3. Crear la clase "Computadora", con 3 atributos (procesador, memoriaRAM, almacenamiento), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (pc1, pc2).
4. Crear la clase "Animal", con 3 atributos (especie, nombre, edad), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (animal1, animal2).
5. Crear la clase "Telefono", con 3 atributos (marca, modelo, sistemaOperativo), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (movil1, movil2).
6. Crear la clase "Empleado", con 3 atributos (nombre, puesto, salario), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (empleado1, empleado2).
7. Crear la clase "Estudiante", con 3 atributos (nombre, matricula, carrera), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (estudiante1, estudiante2).
8. Crear la clase "Fruta", con 3 atributos (nombre, color, tipoSabor), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (fruta1, fruta2).
9. Crear la clase "Pelicula", con 3 atributos (titulo, director, duracion), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (pelicula1, pelicula2).
10. Crear la clase "Ciudad", con 3 atributos (nombre, pais, poblacion), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (ciudad1, ciudad2).
11. Crear la clase "Videojuego", con 3 atributos (nombre, genero, plataforma), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (juego1, juego2).
12. Crear la clase "Instrumento", con 3 atributos (nombre, tipo, material), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (instrumento1, instrumento2).
13. Crear la clase "Mascota", con 3 atributos (nombre, raza, peso), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (mascota1, mascota2).
14. Crear la clase "Avion", con 3 atributos (aerolinea, capacidad, destino), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (avion1, avion2).
15. Crear la clase "Deporte", con 3 atributos (nombre, numJugadores, equipo), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (deporte1, deporte2).
16. Crear la clase "Asignatura", con 3 atributos (nombre, codigo, creditos), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (clase1, clase2).
17. Crear la clase "Edificio", con 3 atributos (direccion, numPisos, color), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (edificio1, edificio2).
18. Crear la clase "Reloj", con 3 atributos (marca, tipo, precio), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (reloj1, reloj2).
19. Crear la clase "Planta", con 3 atributos (nombre, tipoRiego, altura), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (planta1, planta2).
20. Crear la clase "Restaurante", con 3 atributos (nombre, especialidad, calificacion), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (local1, local2).
21. Crear la clase "Bicicleta", con 3 atributos (marca, rodado, material), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (bici1, bici2).
22. Crear la clase "Pintura", con 3 atributos (nombreObra, artista, técnica), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (cuadro1, cuadro2).
23. Crear la clase "Empresa", con 3 atributos (razonSocial, sector, numEmpleados), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (empresa1, empresa2).
24. Crear la clase "Electrodomestico", con 3 atributos (nombre, consumo, color), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (aparato1, aparato2).
25. Crear la clase "Pais", con 3 atributos (nombre, continente, idioma), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (pais1, pais2).
26. Crear la clase "Mueble", con 3 atributos (tipo, material, dimensiones), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (mueble1, mueble2).
27. Crear la clase "CuentaBancaria", con 3 atributos (titular, numeroCuenta, saldo), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (cuenta1, cuenta2).
28. Crear la clase "Ropa", con 3 atributos (prenda, talla, tela), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (prenda1, prenda2).
29. Crear la clase "Curso", con 3 atributos (tematica, duracion, profesor), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (curso1, curso2).
30. Crear la clase "Bebida", con 3 atributos (nombre, marca, mililitros), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (bebida1, bebida2).

¡Mucho éxito con tu práctica de programación!
¡Claro que sí! Aquí tienes una segunda lista con otros 30 ejercicios prácticos, numerados del 31 al 60, siguiendo la misma estructura para que sigas practicando la creación de clases y objetos:

31. Crear la clase "Televisor", con 3 atributos (marca, pulgadas, tecnologia), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (tv1, tv2).
32. Crear la clase "Bote", con 3 atributos (nombre, eslora, material), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (bote1, bote2).
33. Crear la clase "Medicamento", con 3 atributos (nombre, compuesto, dosis), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (med1, med2).
34. Crear la clase "Herramienta", con 3 atributos (nombre, tipo, marca), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (herramienta1, herramienta2).
35. Crear la clase "Zapato", con 3 atributos (marca, talla, color), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (zapato1, zapato2).
36. Crear la clase "Planeta", con 3 atributos (nombre, masa, distanciaAlSol), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (planeta1, planeta2).
37. Crear la clase "Lámpara", con 3 atributos (tipo, potencia, material), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (lampara1, lampara2).
38. Crear la clase "Estadio", con 3 atributos (nombre, capacidad, ciudad), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (estadio1, estadio2).
39. Crear la clase "Canción", con 3 atributos (titulo, artista, album), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (cancion1, cancion2).
40. Crear la clase "Cámara", con 3 atributos (marca, resolucion, tipoLente), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (camara1, camara2).
41. Crear la clase "Perfume", con 3 atributos (nombre, marca, fragancia), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (perfume1, perfume2).
42. Crear la clase "Bosque", con 3 atributos (nombre, ubicacion, tipoArboles), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (bosque1, bosque2).
43. Crear la clase "Ascensor", con 3 atributos (marca, capacidadCarga, numPisos), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (ascensor1, ascensor2).
44. Crear la clase "Juguete", con 3 atributos (nombre, edadRecomendada, material), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (juguete1, juguete2).
45. Crear la clase "Puente", con 3 atributos (nombre, longitud, tipoConstruccion), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (puente1, puente2).
46. Crear la clase "Revista", con 3 atributos (titulo, edicion, tematica), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (revista1, revista2).
47. Crear la clase "Montaña", con 3 atributos (nombre, altura, cordillera), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (montaña1, montaña2).
48. Crear la clase "Batería", con 3 atributos (marca, voltaje, capacidad), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (pila1, pila2).
49. Crear la clase "Mochila", con 3 atributos (marca, capacidadLitros, color), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (bolso1, bolso2).
50. Crear la clase "Idioma", con 3 atributos (nombre, numHablantes, region), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (idioma1, idioma2).
51. Crear la clase "Sofá", con 3 atributos (material, plazas, color), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (sofa1, sofa2).
52. Crear la clase "Teclado", con 3 atributos (marca, tipoSwitch, idiomaLayout), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (teclado1, teclado2).
53. Crear la clase "Panadería", con 3 atributos (nombre, direccion, especialidad), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (local1, local2).
54. Crear la clase "Insecto", con 3 atributos (nombre, habitat, tieneAlas), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (insecto1, insecto2).
55. Crear la clase "BicicletaElectrica", con 3 atributos (marca, autonomia, motor), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (ebici1, ebici2).
56. Crear la clase "Museo", con 3 atributos (nombre, ciudad, temaPrincipal), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (museo1, museo2).
57. Crear la clase "Lente", con 3 atributos (graduacion, marca, material), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (lente1, lente2).
58. Crear la clase "Clima", con 3 atributos (region, temperaturaMedia, humedad), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (clima1, clima2).
59. Crear la clase "Dinosaurio", con 3 atributos (especie, periodo, alimentacion), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (dino1, dino2).
60. Crear la clase "Microscopio", con 3 atributos (marca, aumentoMax, tipoLuz), 2 funciones leerDatos(), mostrarDatos() y 2 objetos (equipo1, equipo2).

¡Espero que estos nuevos ejercicios te sean de gran utilidad para seguir perfeccionando tus habilidades!
