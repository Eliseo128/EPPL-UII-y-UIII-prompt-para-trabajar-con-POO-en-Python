Aquí tienes 20 ejemplos de clases con 4 atributos y 3 funciones cada una:

1. **Paciente**
   - Atributos: nombre, edad, enfermedad, tipoSangre
   - Funciones: agendarCita(), recetarMedicamento(), realizarExamen()

2. **Producto**
   - Atributos: nombre, precio, categoria, stock
   - Funciones: calcularDescuento(), verificarDisponibilidad(), actualizarStock()

3. **Libro**
   - Atributos: titulo, autor, genero, numeroPaginas
   - Funciones: abrir(), marcarPagina(), buscarCapitulo()

4. **Vehiculo**
   - Atributos: marca, modelo, año, kilometraje
   - Funciones: encender(), acelerar(), frenar()

5. **Empleado**
   - Atributos: nombre, cargo, salario, fechaIngreso
   - Funciones: registrarHoras(), solicitarVacaciones(), generarReporte()

6. **CuentaBancaria**
   - Atributos: numeroCuenta, titular, saldo, tipoMoneda
   - Funciones: depositar(), retirar(), consultarSaldo()

7. **Restaurante**
   - Atributos: nombre, ubicacion, capacidad, tipoCocina
   - Funciones: recibirReserva(), prepararPedido(), facturar()

8. **Mascota**
   - Atributos: nombre, especie, edad, propietario
   - Funciones: alimentar(), pasear(), vacunar()

9. **Smartphone**
   - Atributos: marca, modelo, capacidadAlmacenamiento, sistemaOperativo
   - Funciones: llamar(), tomarFoto(), instalarApp()

10. **Pelicula**
    - Atributos: titulo, director, duracion, clasificacion
    - Funciones: reproducir(), pausar(), calificar()

11. **Hotel**
    - Atributos: nombre, estrellas, numeroHabitaciones, ubicacion
    - Funciones: reservarHabitacion(), hacerCheckIn(), servicioHabitacion()

12. **Planta**
    - Atributos: nombreCientifico, tipo, fechaSiembra, necesitaAgua
    - Funciones: regar(), podar(), fertilizar()

13. **Evento**
    - Atributos: nombre, fecha, lugar, capacidadMaxima
    - Funciones: registrarAsistente(), enviarInvitacion(), generarEntrada()

14. **InstrumentoMusical**
    - Atributos: nombre, familia, material, origen
    - Funciones: afinar(), tocar(), guardar()

15. **Receta**
    - Atributos: nombre, tiempoPreparacion, dificultad, porciones
    - Funciones: listarIngredientes(), mezclar(), hornear()

16. **PaqueteEnvio**
    - Atributos: codigoRastreo, peso, destinatario, estado
    - Funciones: embalar(), etiquetar(), rastrear()

17. **CursoOnline**
    - Atributos: titulo, instructor, duracionHoras, nivel
    - Funciones: inscribirEstudiante(), subirContenido(), evaluar()

18. **Clima**
    - Atributos: ciudad, temperatura, humedad, condicion
    - Funciones: predecir(), alertar(), historial()

19. **Cancion**
    - Atributos: titulo, artista, album, duracion
    - Funciones: reproducir(), agregarPlaylist(), compartir()

20. **Zoologico**
    - Atributos: nombre, areaTotal, numeroEspecies, horarioApertura
    - Funciones: alimentarAnimales(), guiarTour(), mantenerHabitat()


Aquí tienes 30 ejemplos adicionales, numerados del 21 al 50:

21. **Avion**
    - Atributos: modelo, aerolinea, capacidadPasajeros, autonomiaVuelo
    - Funciones: despegar(), aterrizar(), anunciarDestino()

22. **Bicicleta**
    - Atributos: marca, tipo, numeroVelocidades, peso
    - Funciones: pedalear(), cambiarMarcha(), inflarLlantas()

23. **Cafetera**
    - Atributos: marca, capacidadLitros, tipoFiltro, potencia
    - Funciones: calentarAgua(), molerGranos(), servirTaza()

24. **Celula**
    - Atributos: tipo, tamano, organulos, estadoMetabolico
    - Funciones: dividirse(), sintetizarProteinas(), transportarNutrientes()

25. **Ciudad**
    - Atributos: nombre, poblacion, pais, altitud
    - Funciones: planificarTransporte(), recaudarImpuestos(), promoverTurismo()

26. **Computadora**
    - Atributos: procesador, memoriaRAM, almacenamiento, tarjetaGrafica
    - Funciones: encender(), ejecutarPrograma(), conectarInternet()

27. **Dentista**
    - Atributos: nombre, especialidad, consultorio, experienciaAnos
    - Funciones: examinarBoca(), realizarLimpieza(), colocarOrtodoncia()

28. **Documento**
    - Atributos: titulo, autor, fechaCreacion, formato
    - Funciones: editar(), guardar(), imprimir()

29. **Electrodomestico**
    - Atributos: nombre, marca, consumoEnergia, garantiaAnos
    - Funciones: encender(), programarTemporizador(), diagnosticarFallo()

30. **Enfermera**
    - Atributos: nombre, turno, area, anosExperiencia
    - Funciones: tomarSignosVitales(), administrarMedicamento(), actualizarHistoria()

31. **Escuela**
    - Atributos: nombre, nivelEducativo, numeroAlumnos, direccion
    - Funciones: matricularAlumno(), asignarCalificacion(), organizarEvento()

32. **Farmacia**
    - Atributos: nombre, licencia, horario, inventarioMedicamentos
    - Funciones: dispensarReceta(), asesorarCliente(), controlarInventario()

33. **Gimnasio**
    - Atributos: nombre, capacidad, tipoMembresia, equipamiento
    - Funciones: registrarEntrada(), asignarRutina(), medirProgreso()

34. **Heladeria**
    - Atributos: nombre, saboresDisponibles, temperaturaFreezer, ubicacion
    - Funciones: prepararHelado(), servirCono(), cobrarPedido()

35. **Inmueble**
    - Atributos: direccion, metrosCuadrados, numeroHabitaciones, precio
    - Funciones: mostrar(), negociarPrecio(), realizarContrato()

36. **Jardin**
    - Atributos: tamano, tipoSuelo, exposicionSolar, plantasActuales
    - Funciones: sembrar(), regar(), podar()

37. **Libreria**
    - Atributos: nombre, catalogoLibros, horarioAtencion, numeroEmpleados
    - Funciones: ordenarEstanteria(), recomendarLectura(), procesarVenta()

38. **Maquina**
    - Atributos: tipo, marca, anoFabricacion, estadoOperativo
    - Funciones: encender(), calibrar(), realizarMantenimiento()

39. **NaveEspacial**
    - Atributos: nombre, tripulacionMaxima, combustible, mision
    - Funciones: despegar(), orbitar(), transmitirDatos()

40. **ObraArte**
    - Atributos: titulo, artista, anoCreacion, tecnica
    - Funciones: exhibir(), restaurar(), tasvalor()

41. **Parque**
    - Atributos: nombre, extensionHectareas, tipoVegetacion, atractivos
    - Funciones: mantenerAreasVerdes(), organizarActividad(), vigilarSeguridad()

42. **Quirofano**
    - Atributos: numeroSala, equipamiento, estadoEsterilidad, capacidadPersonal
    - Funciones: prepararInstrumental(), esterilizar(), programarCirugia()

43. **Reloj**
    - Atributos: marca, tipoMecanismo, materialCorrea, resistenciaAgua
    - Funciones: ajustarHora(), configurarAlarma(), cronometrar()

44. **SalaCine**
    - Atributos: numeroSala, capacidadButacas, tipoPantalla, sistemaSonido
    - Funciones: proyectarPelicula(), ajustarIluminacion(), limpiarButacas()

45. **Teatro**
    - Atributos: nombre, aforo, tipoEscenario, historiaAnos
    - Funciones: ensayarObra(), venderEntradas(), montarEscenografia()

46. **Universidad**
    - Atributos: nombre, rector, numeroFacultades, rankingMundial
    - Funciones: admitirEstudiante(), investigar(), publicarPaper()

47. **Veterinario**
    - Atributos: nombre, especialidad, clinica, licencia
    - Funciones: diagnosticar(), operar(), vacunar()

48. **Yate**
    - Atributos: nombre, eslora, tripulacion, puertoBase
    - Funciones: navegar(), anclar(), organizarEvento()

49. **Zapateria**
    - Atributos: nombre, marcasDisponibles, numeroTallas, ubicacion
    - Funciones: medirPie(), recomendarModelo(), realizarCambio()

50. **AgenciaViajes**
    - Atributos: nombre, destinosOfrecidos, asesoresDisponibles, certificaciones
    - Funciones: cotizarPaquete(), reservarVuelo(), emitirSeguro()

51. **Banco**
    - Atributos: nombre, numeroSucursales, activosTotales, calificacionRiesgo
    - Funciones: otorgarCredito(), invertirFondos(), emitirTarjeta()

52. **Carniceria**
    - Atributos: nombre, tipoCarnes, proveedores, temperaturaCamara
    - Funciones: cortarCarne(), empacar(), pesarProducto()

53. **Drogueria**
    - Atributos: nombre, inventario, farmaceuticoEncargado, serviciosDomicilio
    - Funciones: surtirReceta(), asesorarDosis(), controlarCaducidad()

54. **EstacionGasolina**
    - Atributos: marca, numeroBombas, precioLitro, serviciosAdicionales
    - Funciones: surtirCombustible(), lavarAuto(), revisarNiveles()

55. **Floristeria**
    - Atributos: nombre, variedadFlores, horarioEntrega, arreglosDisponibles
    - Funciones: elaborarRamo(), enviarDomicilio(), cuidarFlores()

56. **GaleriaArte**
    - Atributos: nombre, curador, espacioExhibicion, artistasRepresentados
    - Funciones: montarExposicion(), venderObra(), organizarVernissage()

57. **Hospital**
    - Atributos: nombre, numeroCamas, especialidades, nivelEmergencias
    - Funciones: atenderUrgencia(), programarCirugia(), gestionarAmbulancia()

58. **Imprenta**
    - Atributos: nombre, capacidadImpresion, tiposPapel, maquinaria
    - Funciones: diseñar(), imprimir(), encuadernar()

59. **Joyeria**
    - Atributos: nombre, materialesTrabajo, orfebre, certificaciones
    - Funciones: diseñarPieza(), tallarGema(), limpiarJoyas()

60. **Kiosco**
    - Atributos: nombre, productosVariados, horarioAtencion, ubicacion
    - Funciones: surtirInventario(), atenderCliente(), recargarSaldo()
